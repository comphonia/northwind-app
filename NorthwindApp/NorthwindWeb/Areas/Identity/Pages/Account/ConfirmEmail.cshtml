@page
@model ConfirmEmailModel
@{
    ViewData["Title"] = "Account Confirmed!";
}

<h2 class="text-center mt-5 my-4">@ViewData["Title"].ToString().ToUpper()</h2>
<a class="text-center" asp-area="Identity" asp-page="/Account/Login">Login</a>